<style>
.custom-gift-box-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background-color: #fafafa;
  color: #000;
  padding: 5px 0px;
  text-decoration: none;
  font-weight: 600;
  font-size: 16px;
  transition: background-color 0.3s ease, transform 0.2s ease;
  width: 100%;
  justify-content: center;
  position: relative; /* Required for shine effect */
  overflow: hidden;   /* Required for shine effect */
  z-index: 0;         /* Ensure proper layering */
}

.custom-gift-box-btn:hover {
  background-color: #c4c4c4; /* Darker background on hover */
}

/* Shine layer */
.custom-gift-box-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 150%;
  height: 100%;
  background: linear-gradient(
    90deg,
    rgba(255, 255, 255, 0) 50%,
    rgba(255, 255, 255, 0.4) 70%,
    rgba(255, 255, 255, 0) 80%
  );
  transform: translateX(-100%);
  z-index: 1;
  pointer-events: none;
}

/* Animate shine on hover */
.custom-gift-box-btn:hover::before {
  animation: smooth-shine 2s ease-in-out infinite;
}

/* Ensure inner content is above shine */
.custom-gift-box-btn > * {
  position: relative;
  z-index: 2;
}

/* Shine animation keyframes */
@keyframes smooth-shine {
  0% {
    transform: translateX(-100%);
  }
  50% {
    transform: translateX(100%);
  }
  100% {
    transform: translateX(100%);
  }
}

.custom-gift-box-btn .gift-icon {
  font-size: 18px;
}
.custom-gift-box-btn .gift-text {
    text-transform: uppercase;
    font-family: 'Garet', sans-serif !important;
    font-weight: bold;
}

.custom-gift-box-btn .gift-arrow {
  background: #000;
  color: #fff;
  border-radius: 50%;
  padding: 0px 5px;
  font-size: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid #fff;
}

@media (max-width: 768px) {
  .custom-gift-box-btn {
    font-size: 14px;
    padding: 10px 16px;
    gap: 15px;
  }

  .custom-gift-box-btn .gift-icon {
    font-size: 16px;
  }

  .custom-gift-box-btn .gift-arrow {
    font-size: 12px;
    padding: 4px 6px;
  }
}

@media (max-width: 480px) {
  .custom-gift-box-btn {
    width: 100%;
    justify-content: center;
    font-size: 13px;
    padding: 10px;
  }
}


</style>
<a href="/pages/custom-gift-box" class="custom-gift-box-btn">
  <span class="gift-icon">🎁</span>
  <span class="gift-text">Create Your Customize Gift Box</span>
  <span class="gift-arrow">➔</span>
</a>

{% schema %}
{
  "name": "Cus Gift Box A.Bar",
  "tag": "section",
  "class": "section",
  "settings": [],
  "presets": [
    {
      "name": "Cus Gift Box A.Bar"
    }
  ]
}
{% endschema %}
